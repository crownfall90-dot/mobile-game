# Передача разработки Vita — Codex → Opus

Дата: 2026-09-25. Ветка `codex/family-home` от PR #2 (исходный commit `e6efce6`).
Репозиторий: https://github.com/crownfall90-dot/mobile-game
Сначала `git status`, затем этот файл и `docs/FAMILY_HOME.md`.

## Готово в первом акте

- Новый стартовый экран Vita и семейная квартира с видимыми мамой и дочерью.
- 10 этапов ремонта с кнопками на конкретных вещах и отдельными JSON-уровнями.
- Спасение семьи в общей защищаемой зоне, проверка опасности перед победой.
- Победа чинит ровно следующий связанный предмет и сохраняется сразу; выход,
  поражение и повтор не выдают второй ремонт.
- Меняются предметы, настроение и одежда семьи. Есть анимация починки.
- Настройки, пауза, подсказка, монеты, звёзды и магазин видимого декора.
- Новая иконка, Android-имя Vita, versionCode 4; новый идентификатор
  `com.crownfall90.vita` не конфликтует со старой игрой.
- Набросок переходной сцены второго акта есть, но большой дом и двор ещё не
  реализованы. Не считай второй акт готовым по наличию `story_screen.gd`.

## Проверено

- `godot --headless --path . -- --home-selfcheck`: пройдены проверки связи
  уровня и предмета, одноразовости ремонта/покупки и сохранения/загрузки.
- `tools/verify.py home_01 ... home_10 --quick`: все 10 уровней прошли G1–G4,
  включая пять режимов правильного решения и неверные порядки.
- `godot --headless --path . -- --smoke`: 8 экранов/окон, автопобеда, 0 ошибок.
- Скриншоты квартиры и головоломки осмотрены на разрешении 450×800.
- Полный перебор G5–G7 выполнен. 4 из 10 уровней прошли все требования
  (`home_01`, `home_05`, `home_07`, `home_10`). У шести остальных единственная
  проблема — G6: слишком много порядков штырей приводят к победе. Нужно
  усложнить именно дизайн этих уровней, не ослаблять порог проверки.
- На телефоне 0.3.0 не установилась из-за конфликта пакетов. В 0.3.1
  сменён package id; пользователь установил её, поиграл и сообщил, что
  квартира выглядит слишком мелкой и схематичной. Это и вызвало редизайн.
- В будущем сохранять один и тот же ключ подписи для обновлений с этим
  package id. Текущий отладочный ключ хранится локально, в git его не класть.
- APK — тестовая сборка первого акта, не подтверждённый релиз для магазина.
- После отзыва пользователя о визуальном качестве создан новый мультяшный
  интерьер и две версии мамы с дочерью. Главный экран показывает квартиру на
  весь портретный экран. `HomeArt` смешивает рисунки бедной и отремонтированной
  квартиры по маскам предметов; прогресс отдельных ремонтов виден на экране.
  Герои стали крупнее; та же пара используется в головоломках. Исходные
  изображения находятся в `art/home/`, кадры до/после — в `docs/screenshots/`.
- APK 0.3.2: `build/vita-first-act.apk`, 34 445 207 байт (32,9 МиБ), arm64,
  versionCode 5 / versionName 0.3.2, тот же package id и SHA-256 сертификата,
  что у 0.3.1. Подпись v2/v3 проверена. Ссылка:
  https://github.com/crownfall90-dot/mobile-game/releases/download/vita-v0.3.2-android-test/Vita-first-act-v0.3.2-arm64.apk
- `--home-selfcheck` и `--smoke` прошли после изменения экрана; визуально
  осмотрены начальная квартира, первый, второй и последний ремонт.
- Ограничение: картинки предметов меняются масками двух версий интерьера.
  Некоторые переходы между локальными областями могут быть заметны; для
  релизного качества нужны отдельные варианты предметов и дальнейшая работа
  над видом уровней. Запуск 0.3.2 на настоящем телефоне ещё не подтверждён.

## Мультяшный вариант 0.3.3

- По новому отзыву владельца живописный стиль заменён на более простой и яркий
  мультяшный 0+. Перерисованы квартира до/после, мама с дочерью в трёх нарядах,
  магазинный мишка и стартовый кадр снаружи у скромного входа в квартиру.
  У стартового кадра низ — обычная бежевая дорожка без синей полосы.
- На загрузке поверх картинки написано Vita и показана полоса прогресса;
  название появляется по буквам шрифтом Fredoka (OFL), Router плавно переходит
  к квартире. Первая подсказка указывает на телевизор,
  кнопка починки пульсирует. После ремонта показывается короткая реплика семьи.
- Исправлен магазин после смены интерьера: покупаемые растение, мишка и
  фотография видимы поверх комнаты, одежда меняет обеих героинь. Убрана
  недоступная для просмотра покупка цветов двора из первого акта.
- `--home-selfcheck` и `--smoke` пройдены (8 экранов/окон, 0 ошибок);
  визуально просмотрены заставка, квартира до ремонта и полностью готовая
  квартира с покупками на экране 450×800. APK 0.3.3: 37 518 028 байт,
  arm64, versionCode 6, package `com.crownfall90.vita`, подпись v2/v3
  проверена, сертификат тот же. Пользователь ещё не проверял 0.3.3 на телефоне.
  https://github.com/crownfall90-dot/mobile-game/releases/download/vita-v0.3.3-android-test/Vita-first-act-v0.3.3-arm64.apk
- Ограничение: смена предметов пока идёт через региональные маски двух целых
  изображений, местами видны стыки. Следующий художественный этап — отдельные
  слои именно для ремонтируемых предметов и декора. Уровни всё ещё схематичнее
  главного экрана; шесть уровней не выполняют G6.

## Следующие шаги

1. Скачать APK 0.3.4 из релиза, установить поверх 0.3.1–0.3.3 и проверить: старт,
   весь путь из десяти ремонтов, закрытие/запуск, магазин, настройки, звук.
2. Довести визуальное качество уровней и переходов ремонта до качества главного
   экрана. Усложнить `home_02`, `home_03`, `home_04`, `home_06`, `home_08`, `home_09`
   до прохождения G6, затем пересобрать APK и обновить ссылку.
3. После сдачи APK реализовать дом и двор второго акта: отдельная сцена,
   отдельные рабочие предметы, уровни, переход через сюжетные кадры.
4. Затем спланировать и сделать третий акт. Google Play только после трёх
   полных актов и отдельного разрешения владельца.

## Адаптация экрана 0.3.4

- Заставка длится минимум 5 секунд; название и прогресс видны дольше.
- Квартира заполняет экран телефона по ширине и высоте. На высоких дисплеях
  меняется высота сцены и координаты нажимаемых предметов; текст задания
  закреплён внизу, название Vita — справа вверху, настройки и магазин —
  компактными боковыми кнопками. Проверены снимки 450×800 и 450×1000.
- В настройках добавлен сброс ремонта, покупок, монет и звёзд с подтверждением.
  Обычные настройки сохраняются. Самопроверка покрывает сброс, сохранение и
  повторную загрузку; smoke-проверка прошла (8 экранов/окон, 0 ошибок).
- APK 0.3.4: 37 522 124 байта, arm64, versionCode 7,
  `com.crownfall90.vita`, подпись v2/v3 и прежний сертификат проверены.
  На настоящем телефоне 0.3.4 ещё не проверена.
  https://github.com/crownfall90-dot/mobile-game/releases/download/vita-v0.3.4-android-test/Vita-first-act-v0.3.4-arm64.apk
- Следующее: проверить на телефонах с вырезами и разным соотношением сторон;
  переработать визуал головоломок и отдельные предметы ремонта. Подробный
  порядок — `docs/NEXT_PLAN.md`.

## Проверки и сборка

Godot 4.5.1 находится локально в `D:/tools/godot/` (вне git).
Команды в README. Android SDK и JDK 17 — в `D:/tools/vita-build/`.
Шаблон `android_debug.apk` — в пользовательских шаблонах Godot 4.5.1.
Команда сборки: `Godot_v4.5.1-stable_win64_console.exe --headless --path . --export-debug Android build/vita-first-act.apk`.
Не выдавай наличие APK за подтверждение работы на телефоне.

## Лимит и передача

При приближении лимита: обнови эту запись фактами, зафиксируй проверенный код,
`git push origin codex/family-home`. Opus продолжает с текущей ветки, не
повторяя сделанное. Старый `docs/DESIGN.md` и планы про Мирру устарели.

## Продолжение в Claude (25.09.2026, ветка claude/project-thread-x4ht8m, PR #2)

- Сброс прогресса: после сброса `save.bak` перезаписывается сброшенным
  сохранением, поэтому битый `save.json` больше не возвращает старый прогресс.
  `--home-selfcheck` проверяет это (битый файл → восстановление из .bak → 0 ремонтов).
- Перепроектированы `home_02`, `home_03`, `home_04`, `home_06`, `home_08`,
  `home_09`: каждый требует реакции (вода+лава=камень, кислота, мост из камня),
  проверка не ослаблялась.
- Полный `tools/verify.py home_01 … home_10` (G1–G7): 9/10 PASS. `home_07`
  (не менялся) падает на G2: решение при темпе 0.8 с и seed 0 проигрывает лаве.
  В работе исправление геометрии `home_07`.
- `--home-selfcheck` OK, `--smoke` OK (8 экранов, 0 ошибок).
- APK с этими уровнями пока не собирался. Собирать на ноутбуке тем же ключом
  (команда в разделе сборки выше), versionCode поднять до 8.
- На телефоне ничего из этого не проверено.
- Следующее: `home_07` G2, отдельные слои ремонтируемых предметов (п.5 NEXT_PLAN),
  затем единый стиль UI (п.6).

### Точка остановки (25.09.2026, 21:00 UTC)

Остановлено по просьбе владельца. Два незаконченных черновика не сохранены
(исправление `home_07` и вырезание слоёв предметов); их нужно начать заново.
Сделано и отправлено: сброс/`save.bak`, шесть новых уровней, полный verify 9/10.

Следующие шаги по порядку:
1. `home_07`: G2 падает (решение при темпе 0.8 с, seed 0 проигрывает лаве).
   Поправить геометрию JSON (воронка/нижний бортик, как в `home_02`), не трогая
   `tools/verify.py`; полный `python3 tools/verify.py home_07` → PASS.
2. П.5 NEXT_PLAN — слои ремонта: скрипт `tools/cut_repair_layers.py` (Pillow)
   вырезает каждый из 10 предметов из `studio_repaired.png` с альфой по разнице
   с `studio_worn.png` (порог ~35, расширение и размытие краёв). `home_art.gd`
   рисует изношенный фон + слои починенных предметов вместо шейдера с масками;
   короткая анимация появления (~0.6 с). Проверить скриншотами этапов 0/3/6/10
   на 720×1280 и 720×1600, затем `--home-selfcheck` и `--smoke`.
3. П.6 — единый стиль UI (настройки, магазин, пауза, победа, HUD) в палитре Vita.
4. Далее п.4, 7, 9, 10 NEXT_PLAN, затем APK versionCode 8 на ноутбуке тем же ключом.

## Новый первый акт: 4 локации, 19 ремонтов (25.09.2026, Claude)

Готово и отправлено (ветка `claude/project-thread-x4ht8m`):
- `data/act1.json` — комната (4), кухня (5), санузел (5), гостиная (5); у каждой цели свой
  уровень `home_01…home_19`, прямоугольник, слой и эффекты причины. `Home` читает акт из JSON,
  следующая локация открывается после всех ремонтов предыдущей, внутри — любой порядок.
  Старый прогресс (10 ремонтов) переносится один раз на столько же первых целей.
- `LocationView` собирает сцену из отдельных слоёв (фон, сломано/починено, накладки-дыры,
  семья, декор, эффекты кодом). Нет PNG — служебная рамка с подписью. Hub масштабирует сцену
  720×1560 равномерно (cover), зоны нажатия едут вместе со сценой; стрелки между локациями;
  после последнего ремонта локации — радостная сцена и открытие следующей.
- `docs/ART_BRIEF.md` — ТЗ на графику (делает ChatGPT, код её подхватывает по именам файлов).
- Механики: копание земли пальцем (`dirt`, `strokes`, `exit` — семья идёт к двери), сито,
  утопление, анимации падения семьи, победа без обязательных всех засовов, тематические задники.
- UI: одна кнопка «Хорошо» после победы; чистый главный экран; магазин карточками; язык — русский.

Проверено: `--home-selfcheck` (новая модель: порядок открытия, выбор цели, закрытые локации,
перенос старого прогресса, safe-область), `--smoke` (0 ошибок), снимки hub 720×1280 и 720×1600.
Уровни: `home_01` (горка), `home_02` (копание-обучение), `home_03` (копание с ловушкой) — полный
verify PASS. `home_04…home_10` — старые уровни с засовами: после отмены «все засовы обязательны»
`home_02`-подобные fails могут выигрывать — их всё равно заменяют. `home_11…home_19` ещё нет:
hub говорит «головоломка скоро появится». APK не собирался; на телефоне ничего не проверено.

Следующий шаг: головоломки `home_04…home_19` (черновик «потопа» — scratchpad, не в git), затем
единый стиль UI (п.6), подключение графики по мере поступления.

### Прогресс 25.09.2026 (поздно вечером)

- Уровни комнаты и кухни готовы: `home_01…home_09`, полный verify PASS у всех. Засовы:
  01 горка, 04 потоп со сливом, 05 раковина с пробкой и слизнем, 07 дуршлаг с кислотой.
  Копание: 02 обучение, 03 ловушка у двери, 06 слизень в ямке, 08 вилка на две ямы,
  09 лабиринт между камнями. `home_10…home_19` (санузел, гостиная) ещё нет.
- Проверены снимками: переход «ремонт стены → реплика → праздник → кухня» (флаг
  `--screen-args=repaired:room_wall`), уровни на весь экран 720×1280 и 720×1600.
- Против вылета на слабом Xiaomi (на устройстве не воспроизведено): сжатие текстур ETC2,
  без предзагрузки больших картинок, старый экран освобождается до нового, слой жидкостей
  отвязывается перед удалением, журнал ошибок копируется из настроек. Проверить на телефоне.
- Физика: жидкости с непрерывной проверкой столкновений (не проскакивают засов); тонкие
  остатки земли убираются; «застряли» на уровнях с дверью — только когда всё успокоилось.
- Графика: ChatGPT работает по `docs/ART_BRIEF.md`, переписка — issue #3. Ракурс 45°.
- Следующее: головоломки санузла и гостиной (`home_10…home_19`), затем единый стиль UI,
  затем подключение картинок по мере поступления. APK собирает владелец (versionCode 8).

### Прогресс 26.09.2026 — головоломки «внутри вещи», адаптивность, сценка кухни

Готово:
- Исправлена ошибка компиляции `location_view.gd:272` (hub не открывался на ноутбуке и у ChatGPT).
- Режим «вещь вместо семьи»: `receiver` в JSON уровня (`scripts/level/receiver.gd` — слив,
  конфорка, ведро, ящик, дыра). Победа чинит саму вещь (стенки блестят, конфорка горит голубым).
  Переделаны `home_05` (плита, огонь к конфорке мимо воды) и `home_06` (раковина: лава → засор,
  вода → лава, потом пробка). Кухня: плита → `home_05`, раковина → `home_06`.
- Адаптивность hub: масштаб cover, но не больше, чем позволяет safe-область
  (`scene.safe` в act1.json) — на планшетах, раскладушках и 21:9 все цели видны; за краем
  сцены — продолжение крайних пикселей фона чуть темнее. Кнопки hub по меньшей стороне экрана.
- Подсказка уровня — в полосе между верхней панелью и полем, если она ≥ 64 px, иначе прежнее место.
- Фон головоломки `art/act1/levels/<theme>.png` рисуется 1:1 со сценой (1440 px = 720), поле
  картинки 260–1180 × 400–2420 (2x) ложится центром на `tower.rect`; раньше картинка
  увеличивалась ×1.47, и ChatGPT видел белый фон. Картинка грузится в `setup`, не в `_draw`.
- Сценки: `props` в локации act1.json — отдельные слои `{img, rect, z}`, рисуются вместе с целями
  по z. Нет ключа `family` — пара не рисуется. Кухня: мама у плиты с кастрюлей (z3), стул (z3),
  дочка на стуле (z4), стол (z5); холодильник/плита/раковина сдвинуты, никто никого не закрывает.

Проверено: `--home-selfcheck` OK, `--smoke` OK, lint 0 ошибок, quick verify 11/11 (до этих правок
уровни не менялись, кроме текста подсказки `home_06`). Снимки hub, кухни и уровней на 720×1280,
720×1440, 720×1560, 720×1600, 720×1680, 720×1800, 900×1280, 1080×1200, 1200×1600 (xvfb).
На телефоне не проверено.

Ограничения: на кухне мама одна и та же при любом настроении (нет вариантов mood); покупка
«одежда семьи» в кухне не видна. `family_mood0` стоит на 8 px ниже, чем `mood1–3` (сообщено ChatGPT).
`home_01–04`, `07–11` ещё с семьёй внутри уровня — их нужно перевести в режим вещи.

Следующий шаг: перевести `home_07` (холодильник) в режим вещи тем же способом, что `home_05/06`
(генератор уровней — scratchpad `lv/lvlib.py`, `item_level`), затем 08, 09, 01–04, 10–11, потом
новые `home_12…home_19`.

### Прогресс 26.09.2026 (ночь) — нарушители, окно без семьи, APK 0.5.0

Готово:
- Нарушители под каждую вещь (`docs/CAST.md`, решение владельца): крыса, мышь, паук с паутиной,
  моль (уходят от воды), засор, плесень, таракан (от чистящего средства — это `acid`). Никто не
  гибнет: убегают, улетают, смываются, растворяются. Лава — только огонь плиты.
  Картинки нарушителей заказаны ChatGPT (`art/act1/enemies/<kind>.png`, ТЗ в ART_BRIEF);
  пока их нет — заглушки кодом в `enemy.gd`.
- `home_06` раковина: средство растворяет засор, если вода его не разбавила; поле подогнано под
  `sink.png`, вода уходит в нарисованный слив (`look: art`, невидимое дно под ним).
- `home_01` окно (обучение): дождь по горкам в отлив, без семьи.
- Окно победы в уровнях-вещах: «Починено!» и «Ремонт: <вещь> — готово!».
- Версия 0.5.0 (versionCode 9).

Проверено: полный verify `home_01`, `home_06` — PASS; quick verify всех 11 — PASS; smoke, selfcheck.
Снимки уровней на 720×1280. На телефоне не проверено.

APK 0.5.0 собран в облаке и лежит в отдельной ветке `apk-builds` (по разрешению владельца):
https://github.com/crownfall90-dot/mobile-game/blob/apk-builds/Vita-0.5.0.apk
Он подписан облачным тестовым ключом, не ключом с ноутбука: перед установкой старую Vita нужно
удалить. Ключ лежал во временной папке сессии и потерян вместе с ней; следующая облачная сборка
снова потребует удаления. Чтобы обновлять без удаления, APK собирает владелец: `vita.bat` → 2.

Как собрать APK в облаке (Android SDK с dl.google.com закрыт политикой сети):
1. Шаблоны: `Godot_v4.5.1-stable_export_templates.tpz` с GitHub → распаковать только
   `templates/android_*` и `version.txt` в `~/.local/share/godot/export_templates/4.5.1.stable/`.
2. Заглушка SDK: папка с `platform-tools/adb` и `build-tools/35.0.0/apksigner` (скрипты-заглушки),
   путь — в `export/android/android_sdk_path` файла `~/.config/godot/editor_settings-4.5.tres`.
3. Временно `package/signed=false` в пресете "Android", `godot --headless --export-release
   "Android" build/vita_unsigned.apk`, вернуть пресет.
4. Подпись и выравнивание: `uber-apk-signer-1.3.0.jar` (GitHub) с ключом из `keytool`.
5. Файл больше 30 МБ в чат не отправляется, артефакты не раздают архивы → ветка `apk-builds`.

Следующий шаг: `home_02` кровать (моль, пыль, пружинки в ящик), `home_03` пол (крыса, щебень
в дыру), `home_04` стена (паук), затем кухня `home_07–09` по `docs/CAST.md`.

### Прогресс 26.09.2026 (день) — новые механики, сюжетные реплики, износ комнаты

Готово (код, уровни, данные):
- Три новые механики «внутри вещи» (выбор владельца из 8):
  - `home_01` окно — «Замазка под дождём»: пальцем рисуешь жёлоб (`scripts/level/putty.gd`,
    ключ `putty` {ink, width}), дождь (`source`) должен уйти в отлив, а не на подоконник (`sill`).
  - `home_04` стена — «Живые трубы»: тап по колену трубы (`scripts/level/pipe_switch.gd`, ключ
    `pipes`) меняет поток; вода смывает паука и уходит в слив, мимо розетки (`socket`).
  - `home_07` холодильник — «Поверни холодильник»: кнопки поворота мира (`rotate`), гравитация
    вращается, средство растворяет плесень.
- `home_02` кровать — обучение копанию (один ход, пружинки в коробку).
- `home_03` пол — крыса в дыре: сначала вода (крыса убегает, вода уходит сквозь щели дна дыры),
  потом щебень заделывает дыру. Щебень раньше воды — «Заделали, а крыса внутри!» (правило
  `walled` у приёмника `mode: fill`). Справа проводка (`hazards` kind `wire`) и лужица над ней.
- Раковина и плита поменялись местами: `home_05` раковина (засор, средство), `home_06` плита.
- verify: режимы `live` (темп человека 1,3/1,7 с) и `draw` (без G5–G7), перебор поворотов,
  трубы как ходы; lint знает `pipes`, `source`, `hazards`, `rotate`, `putty`.
- Хаб: комната «обшарпана» шейдером `shaders/wear.gdshader` и светлеет по мере ремонта
  (пятна у несделанных вещей); несколько дыр и трещины в полу (`more`, `cracks`); персонажи крупнее,
  кухня расставлена естественно; подсказка «Нажми!» рукой после 6 с бездействия; после ремонта
  мама и дочка радуются (прыжок, реплики `cheer` в `data/act1.json`, пузыри `speech_bubble.gd`),
  приветствие при первом входе в локацию.
- Иконка «Мама и дочка в окне» (`tools/make_icons.gd`, `art/app/`).
- Связь с ChatGPT: issue #3, ежечасная проверка (Routine «Vita: сообщения ChatGPT»).

Проверено: полный verify `home_01`–`home_07` — PASS (у `home_02` исключение G6: один ход);
quick verify всех уровней act1 — PASS; lint, smoke, `--home-selfcheck` — OK. Снимки 720×1280.
На телефоне не проверено.

Ограничения: `home_08`–`home_11` — ещё старые семейные уровни; `home_12`–`home_19` нет.
Старый `f1_05` не входит в `levels/index.json` (в `profile.gd` остался только его id для переноса старых
сохранений). С коммита 4039624 (непрерывные столкновения жидкостей) он проигрывал в G2 при jitter 0,
поэтому перенесён в `levels/legacy/`, чтобы общий verify проверял только уровни игры.

«Лови капли» — `home_09` потолок кухни (фон `ceiling.png`), модуль `scripts/level/leak.gd`:
- из дыр трубы капает; ведро (приёмник `look: bucket`) ездит за пальцем внизу и ловит капли;
  промахи уходят в дыру к кухне (`hazards` kind `leak`, предел `limit`), звёзды: 3 − промахи;
- дыру заклеивают, подержав на ней палец `hold` с; мышь бежит по трубе и грызёт новые дыры,
  пока грызёт — её можно спугнуть тапом, после `scares` испугов уходит насовсем;
- двумя пальцами можно одновременно держать дыру и водить ведро; без касаний протечка
  начинается сама через `auto` с; кнопка подсказки подсвечивает открытую дыру;
- проверка: ходы — сценарии по времени (`scripts`: move/press/up/scare), DevRunner
  проигрывает их, при jitter сдвигает события на ±0,06 с; verify считает уровень «живым»
  без G5/G6. Полный verify `home_09` — PASS (решение 5/5, «ничего не делать» и «только
  заклеить» проигрывают).

«Стопка посуды» — `home_08` шкафы (фон `cabinet.png`), модуль `scripts/level/dishes.gd`:
- полки качаются, каждая на одном винте (пружина к ровному положению); посуда по одной
  (тарелка, чашка, миска, кастрюля) появляется сверху, палец ведёт, отпустил — падает;
- перекосило сильнее 22° дольше 0,3 с — винт вырывает, полка падает («Полку перекосило»);
  посуда на дне шкафа — «Посуда разбилась»; винт краснеет, когда полку клонит;
- таракан сидит у винта верхней полки и убегает, когда рядом встала посуда;
- после победы полки и посуда замирают; сценарий робота — `[t, "drop", x, y]`.
Полный verify `home_08` — PASS (перегруз края и бросок на дно проигрывают).

Санузел: слои ChatGPT подключены и расставлены (`data/act1.json`), проверено предпросмотром.

### Прогресс 26.09.2026 (утро) — санузел целиком, гостиная собрана

Санузел — все 5 головоломок на фонах ChatGPT, полный verify каждой — PASS:
- `home_10` ванна (`tub.png`): «Поворот» с шагом 25° и пределом (`rotate`: {step, max}):
  наклон вправо — средство из мыльницы в шахту слива на засор, потом вода; из 16
  последовательностей выигрывает одна.
- `home_11` унитаз (`toilet.png`): засов со средством P, колено A (вода в перелив, пока не
  повернёшь), колено-ловушка C; засор набухает от воды (флаг врага `swell`, проигрыш
  `swelled`); перелив — опасность `spill` (трубка и короб с глухим дном).
- `home_12` раковина (`bath_sink.png`): новая механика «Вантуз» (`scripts/level/plunger.gd`):
  тап в ритм толкает засор по нарисованному сифону; раньше времени — выплеск (3 — проигрыш),
  медленно — засор сползает, раковина переполняется за 22 с.
- `home_13` плитка (`tiles.png`): «Замазка 2» — два шва мимо розетки и плесени (новая
  опасность `mold`, пятно растёт от капель).
- `home_14` светильник (`bath_light.png`): новая механика «Луч и зеркальца»
  (`scripts/level/mirrors.gd`), вид `current`: ток от провода через контакты к патрону,
  моль на контакте, батарейка на 6 поворотов, par 4.
- Сцена санузла: отдельные позы (мама отжимает ткань над тазом, дочка с корабликом).
- Гостиная: мама сидит на диване с книгой, дочка рисует на полу (`props`), мебель позади.
  ChatGPT проверил цельный кадр 450×1000 и принял расстановку (issue #3, 26.09 05:22 UTC,
  его коммит df4a5bd); стартовый пузырь реплики ненадолго закрывает книгу и ТВ — это интерфейс.
- Фоны всех 19 головоломок у ChatGPT готовы; посуда и `wire_box.png` подключены.
- Старый `f1_05` перенесён в `levels/legacy/` (не в индексе; падал с коммита 4039624).

Проверено: lint, quick verify всех уровней act1, smoke, `--home-selfcheck`, снимки 720×1280
(и 720×1600 для локаций). На телефоне не проверено.

Урок: картинку для рисования грузить заранее (`_ready`/`setup`), не внутри `_draw` —
иначе первый кадр белый и не перерисовывается.

### Прогресс 26.09.2026 (день) — весь первый акт, APK 0.6.0

Гостиная — все 5 головоломок на фонах ChatGPT, полный verify всех 19 уровней — PASS (G1–G7):
- `home_15` диван (`sofa.png`): новая механика «Сшей диван» (`scripts/level/sew.gd`): стежки
  зигзагом по парам дырок, неверная дырка — узелок (3 — проигрыш), пружины прячутся тапом,
  мышь распускает последний стежок, если её не спугнуть.
- `home_16` телевизор (`tv.png`): зеркала, вид `signal`, паук на контакте (`blocker`), экран
  картинки загорается (`screen`).
- `home_17` люстра (`living_lamp.png`): зеркала, вид `current`, две моли (`moths`).
- `home_18` стена (`living_wall.png`): «Живые трубы 3» — отопление, приёмник `radiator`,
  бак-отстойник, розетка у опасного выхода, плесень под ловушкой-коленом.
- `home_19` пол (`living_floor.png`): копание, две крысы (норка на пути воды и дыра), проводка.
- Исправлено: нарушители, смотрящие вправо (и мыши в «Лови капли»/«Сшей диван»), рисовались
  на ширину спрайта в стороне — Godot сдвигает прямоугольник с отрицательной шириной; теперь
  отражение масштабом `draw_set_transform`. У дыры (mode fill) дно есть при любом виде.

APK 0.6.0 (versionCode 10), ~82 МБ: https://github.com/crownfall90-dot/mobile-game/blob/apk-builds/Vita-0.6.0.apk
Подписан тем же облачным ключом, что 0.5.0 (`scratchpad/apk/vita-cloud.jks`, алиас `vita`) — ставится
поверх 0.5.0. Ключ живёт только в папке этой облачной сессии.
Сборка в облаке — как выше, но экспорт запускать без подмены `XDG_DATA_HOME` (шаблоны лежат в
`~/.local/share/godot/export_templates`). Предел GitHub — 100 МБ на файл: 19 фонов головоломок
импортируются с `process/size_limit=2048` (≈0,97 МБ вместо 2,25 МБ каждый, картинка рисуется по
ширине 720 и от размера в пикселях не зависит); фоны локаций — в полном размере.
На телефоне не проверено.

## Прогресс 26.09.2026 (утро, сюжет)

Готово и проверено (smoke OK, `--home-selfcheck` OK, снимки хаба 720×1280/1600, все 19 уровней
открываются без ошибок):
- **Хмурь** (`scripts/art/gloom.gd`, подключена в `hub_screen.gd`): серое облачко под потолком
  каждой локации. Размер и морось — по доле несделанных ремонтов; ремонт — облачко съёживается,
  локация готова — улетает. После всего акта — белое облачко-друг. Тап — реплики
  (`data/act1.json`: у локации `gloom: {pos, lines}`, общий `gloom_friend`). Сломанная вещь под
  пальцем важнее Хмури. Пока облачко рисует код; картинки `art/act1/story/gloom_grey.png` и
  `gloom_white.png` подхватятся сами.
- Пузырь реплики умеет хвостик вверх (`below`): реплика Хмури — под ней, не под панелью сверху.
- **Фоны головоломок:** над сдвинутым полем (home_16/17/18, home_09) растянутая полоска края
  давала вертикальные полосы (замечание ChatGPT, c8f07d6). Теперь поля за картинкой —
  приглушённое зеркальное продолжение с тенью и светлой кромкой (`home_backdrop._mirror_edge`),
  привязка картинки к уровню прежняя.
- Снимки проверок ChatGPT (`art/act1/reviews/*`) исключены из экспорта APK.
- ChatGPT принял расстановку гостиной (5843498619); визуальную приёмку головоломок гостиной
  закроет после повторного кадра с исправленным фоном.

## Прогресс 26.09.2026 (день): сюжет — визуальная новелла

Владелец: «в стиле новеллы будем делать». Сделано и проверено (smoke OK, `--home-selfcheck` OK,
lint 0 ошибок, все 6 сцен доигрываются автопролистыванием без ошибок скриптов, снимки 720×1280):
- `scripts/screens/novel_screen.gd` — экран `novel` (старый `story_screen.gd` от прежнего замысла
  удалён): фон — комната как на хабе без семьи (или `art/act1/story/<имя>.png`, ночь без картинки
  рисует код, дождь), пара мама+дочка `family_mood0..3` по эмоции реплики, Хмурь (`gloom.gd`,
  `befriend()`), окно диалога с табличкой имени, текст печатается (40 букв/с), тап —
  допечатать/дальше, выбор ответа кнопками, карточки письма и фото из 4 кусочков, «Пропустить»
  и системная «назад». Для проверок: `--screen=novel --screen-args=scene:<id>,step:N` (с шага N)
  и `auto:1` (листается само).
- `data/novel.json` — сценарий: `prologue`, `room_done`, `kitchen_done`, `bath_done`,
  `living_done` → `act1_end`.
- Пролог при первом запуске (`loading_screen`, флаг `seen.prologue`). Конец локации: салют на
  хабе (2,4 с), флаг `photo.<локация>`, сценка `<локация>_done`, затем следующая локация.
  Хмурь на хабе белеет только после финальной сценки (`seen.novel.act1_end`).
- Итоговые реплики локаций (`lines.done`) убраны из `act1.json` — их рассказывает новелла;
  приветствие комнаты больше не повторяет пролог.
- ТЗ ChatGPT на картинки новеллы — `docs/ART_BRIEF.md` («Сюжет акта 1 — визуальная новелла»):
  Хмурь серая/белая, `night.png`, `letter.png`, `photo_full.png`; по желанию отдельные спрайты
  мамы и Виты с эмоциями (`art/act1/novel/`) — их подключение ещё не написано.

- ChatGPT прислал `story/gloom_grey.png` и `gloom_white.png` (ccbf6c2) — подхвачены хабом и
  новеллой, рисуются ×1.2 (у PNG облачко почти во всю ширину). По его повторной проверке фонов:
  зеркало повторяло части предмета — теперь за краем картинки спокойная подложка из 12
  усреднённых цветов края (`home_backdrop._calm_edge/_edge_strip`), снимки home_16/17/18/09.

Ограничения: сцены на снимках проверены на 720×1280; на телефоне не проверено. Кусочки фото
видны только в сценках — альбома для повтора пока нет.

Следующий шаг: альбом (попап из хаба: кусочки фото и повтор просмотренных сценок `seen.novel.*`),
подключение картинок новеллы по мере прихода от ChatGPT; затем звук механик, аудит; ждать отзыва
владельца по APK 0.6.0.

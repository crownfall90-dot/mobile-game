extends Node
## Корневая сцена: только передаёт управление Router, всё остальное строят экраны.


func _ready() -> void:
	# отложенно: к этому моменту все автозагрузки уже готовы
	Router.boot.call_deferred()
